import 'package:flutter/material.dart';

class CBContactCellWidget extends StatelessWidget {
  const CBContactCellWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
